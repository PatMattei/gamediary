const React = require('react');
const Layout = require('./components/Layout');

class Show extends React.Component {
    render() {
        return (
            <Layout>
                <h1>Show</h1>
            </Layout>
        )
    }
}

module.exports = Show;